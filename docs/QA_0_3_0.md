# 0.3.0 体验回归记录

日期：2026-09-25。使用本机 HTTP 服务、Codex 内置浏览器及 390 × 844 手机视口；不修改线上玩家存档。

## 已验证

- 桌面舞台显示原角色，Canvas 的渲染状态为 animated；浏览器无脚本/着色器错误。
- 手机对话时人物固定在上方，文字与选择可操作；退出对话恢复完整舞台。
- 「今天有一点累 → 陪我待一会儿 → 记住这一刻」完成后好感 +4；刷新后保留进度，问候引用上次疲惫。
- 「理理头发」包含玩家选择、继续对白和结尾；完成 +3。
- 重温同一天的相处不重复加分；相处手记保留。
- 在画面上实际点击手部命中「掌心的温度」，未误判到头发或衣领。
- 相处完成后立即刷新，后续短信依然送达；短信页同时显示旧来信和新来信。
- 选择回复显示玩家气泡与输入中状态，随后角色答复；好感达到 10 后解锁剧情。
- 手机剧情逐句推进到第 6 段，选择追查来人后明确显示对应回复，继续到第 9 段完成；好感变为 22，档案已归档。
- 一分钟陪伴计时真实完成后增加 2 点、记录手记并出现结束台词。
- `node scripts/check-interactions.mjs`：旧存档迁移、全部相处节点可达、重复奖励、持久消息、单次回复、完整短信记录、剧情选择/重玩、缩放命中、存储失败、发布元数据均通过。
- 所有 JS 文件语法检查及 Git 空白差异检查通过。

## 检查中修复

- 手机固定底栏遮住剧情继续按钮：为剧情面板和进度条增加底部避让。
- 即时刷新丢失延迟来信：将待发消息改为存档队列。
- 旧缓存版本脚本将 meta 属性替换为 `$10.2.1`：使用带括号的正则分组替换并增加回归断言。

## 仍需后续验证

- 真机 Android/iOS 的性能、电量和不同浏览器音频表现，当前手机测试使用视口模拟。
- GPU 不可用时显示静态后备图；局部形变不具备完整骨骼动作或任意视角。
- 本地保存和网页内定时投递不等同于跨设备同步或系统级推送。
